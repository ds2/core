package ds2.oss.core.api.dto.impl;

import ds2.oss.core.api.environment.ConfigurableItem;

public class ConfigurableItemScanner {
    public static String getValue(ConfigurableItem item, String[] parameters) {
        String rc = null;
        if (parameters != null && parameters.length > 0) {

        }
        return rc;
    }
}
