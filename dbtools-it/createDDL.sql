CREATE TABLE oc_my (ID BIGINT NOT NULL, date DATETIME, NAME VARCHAR(255), VERSION INTEGER, state_id BIGINT NOT NULL, PRIMARY KEY (ID))
CREATE TABLE oc_state (id BIGINT NOT NULL UNIQUE, NAME VARCHAR(255) NOT NULL, PRIMARY KEY (id))
ALTER TABLE oc_my ADD CONSTRAINT FK_oc_my_state_id FOREIGN KEY (state_id) REFERENCES oc_state (id)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
